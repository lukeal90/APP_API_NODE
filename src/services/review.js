
const addReview = (review) => {
    review.save();
};

const updateReview = (rta) => {
    return rta;
}

const deleteReview = (rta) => {
    return rta;
}

module.exports = {
    addReview,
    updateReview,
    deleteReview
};